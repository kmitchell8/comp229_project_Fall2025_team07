import React from 'react'
//import './UpdateBook.css'


const UpdateBook = (/*{pathId}/*{parentSegment}*/) => {//pass the _id path to be able to create an Id subview

    // const bookId = pathId; // Extracts _Id to create a _Id subview (pathId can  also be parentSegment[2])
    //This information is passed down from the profileView segment anytime 
    //anything in the profile.html# path is invoked at any point in the routing   
    //see profileView and Adminview to understand the routing              
    return (<div className='updatebook'>

    </div>)
}

export default UpdateBook